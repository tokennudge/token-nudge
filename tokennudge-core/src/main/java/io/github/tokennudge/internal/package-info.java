/**
 * Internal implementation details of {@code tokennudge-core}.
 *
 * <p>Classes in this package are <strong>not public API</strong>: there is no
 * {@code module-info.java} enforcing this in v1, so the package is exported like any
 * other, but its contents may change or disappear between any two versions without
 * notice. Do not depend on it from outside this module.
 */
package io.github.tokennudge.internal;
