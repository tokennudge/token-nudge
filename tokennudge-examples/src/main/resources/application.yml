spring:
  application:
    name: tokennudge-examples
  datasource:
    url: jdbc:h2:mem:tokennudge-examples;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
    filter:
      create: All tasks
