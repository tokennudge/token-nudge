/**
 * The example service's public HTTP API: {@code POST /orders} and {@code GET /orders/{id}},
 * and their request/response bodies. This is the only surface a black-box test is allowed to
 * call directly.
 */
package io.github.tokennudge.examples.web;
